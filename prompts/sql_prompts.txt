请根据提供的数据库表结构，自动生成该表的 **Top 6** 核心数据分析 SQL 语句和对应的中文描述，用于后续的 Plotly 绘图。

你必须严格以 JSON 数组格式返回结果，数组中每个元素包含 `sql` 和 `description` 字段。
格式要求：
```json
[
  {
    "sql": "SELECT ...", 
    "description": "分析描述 1"
  },
  {
    "sql": "SELECT ...", 
    "description": "分析描述 2"
  },
  ...
]

SQL 语句必须符合以下数据分析和绘图需求：

日期趋势分析：如果涉及时间序列，请计算最近 30 天的趋势（最近指数据库数据中 DATE 列的最大值，而不是使用 CURRENT_DATE）。例如：WHERE date_col >= (SELECT max(date_col) FROM table_name) - INTERVAL '30 days'。

分组限制：如果是类别或其他分组（Group By）求和、求平均的分析，请使用 ORDER BY 和 LIMIT 10 限制输出。

避免中文：SQL 语句中所有的表名和列名必须使用英文名。

数据库包含的表结构如下：
{table_schema}